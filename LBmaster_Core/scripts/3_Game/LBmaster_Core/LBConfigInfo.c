modded class LBConfigInfo {
	void LBConfigInfo() {
		RegisterClassComment("LB_Webhook_Manager", "With this file, you can configure Webhooks for various different events. You can have different webhooks for different events and edit the webhook to look the way you want to\n\nFor how to create a Discord Webhook message, please check out the [Discord Developer API](https://discord.com/developers/docs/resources/webhook#execute-webhook) and use a [Json Escape Tool](https://www.freeformatter.com/json-escape.html) to escape the special characters (for example `\"` will become `\\" + "\"`) Also check the predefined webhooks to see how the formatting works\n\nAlso use a [Json Validator](https://jsonformatter.curiousconcept.com/) to validate the webhooks.json file and also your webhook to make sure it works properly");
		RegisterClassVariableComment("LB_Webhook_Manager", "webhooks", "List of all Webhooks");
		RegisterClassVariableComment("LB_Webhook", "enabled", "Enable / Disable that messages should be sent to the webhook");
		RegisterClassVariableComment("LB_Webhook", "description", "Description to keep track of the Webhooks (only visible from the config)");
		RegisterClassVariableComment("LB_Webhook", "webhookURL", "URL of the Webhook when copying it from the Discord Channel (https://discord.com/api/webhooks/...)");
		RegisterClassVariableComment("LB_Webhook", "webhookMessages", "List of Messages, which should be sent to this webhook. Use the names from below to send only specific messages to have the option for multiple webhooks with different purpose like chat logging and admin logging etc.");
		RegisterClassVariableComment("LB_Webhook", "steamidFilter", "If this list contains any entries, only webhooks associated with this steamid will be sent and the rest is ignored");
		RegisterClassVariableComment("LB_Webhook", "overwrittenMessages", "overwrite any message for example to not include any information, which should not be visible for public channels. An Entry will have the same structure like one entry in the `webhookMessages` list");
		RegisterClassVariableComment("LB_Webhook_Manager", "webhookMessages", "List of the raw Webhook messages with placeholders represented as `$$x$$` where x is a number. See below for information how to setup the message correctly.\\nA complete list of all webhooks can be found [Here](/webhook/list.php)");
		RegisterClassComment("LBAdmins", "This file is used to define the admins for all LBmaster mods.\nAfter adding your Steamid to the file, you can enable Admin mode by pressing `U`\nThis enabled some interaction options to configure items like ATMs or Garages\nTo access the Admin Menu, you need to have admin mode enabled and press `I`\nThese are the default buttons used, which can be changed in the DayZ Controls");
		RegisterClassVariableComment("LBAdmins", "admins", "List of Admins. Each admin needs their own Entry (`{... steamid ...}`)");
		RegisterClassVariableComment("LBAdminsPlayer", "steamid", "The steamid of this admin. Remeber that there can only be one admin per `{... steamid ...}`. Each other Admin will need their own entry with steamid, comment, ...");
		RegisterClassVariableComment("LBAdminsPlayer", "ingameNameForPermissions", "if set to `\"" + "\"` the Admin can join with any name to receive their admin permissions. If set to any value like `\"Peter\"`, the Player needs to join with the ingame name Peter to have admin permissions, otherwise he will be treated as a normal player");
		RegisterClassVariableComment("LBAdminsPlayer", "comment", "Comment field to remember which admin it is, because Steamids are hard to recognize");
		RegisterClassVariableComment("LBAdminsPlayer", "grantAllPermissions", "Set to 1 to give the Admin all available permissions regardless of their permissionGroups");
		RegisterClassVariableComment("LBAdminsPlayer", "permissionGroups", "Permission Groups the admin is associated with. An Admin can be in multiple groups at the same time. There are 4 default groups, but you can add them yourself by adding a new entry in the `groups` list below. The name of that group must exactly match the entry in the list");
		RegisterClassVariableComment("LBAdmins", "groups", "List of different Permission Groups / Ranks (Owner / Admin / Moderator / Supporter etc.)");
		RegisterClassVariableComment("LBAdminsGroup", "name", "Name of the permission group");
		RegisterClassVariableComment("LBAdminsGroup", "permissions", "List of granted permissions (`\"name.of.the.permission\": 1`) for granting permissions or (`\"name.of.the.permission\": 0`) for explicitly revoking a permission, but it's not necessary since all permissions are revoked by default");
		RegisterClassComment("LBAPIConfig", "This file contains info about which API endpoints it should connect to");
		RegisterClassVariableComment("LBAPIConfig", "grantDeveloperAccess", "When enabled, the server will connect to one of the LBmaster API Servers to allow the LBmaster services to perform actions or read data like configs to help troubleshooting. This should be kept disabled unless advised to enable it");
		RegisterClassVariableComment("LBAPIConfig", "entries", "List of all endpoints it should try to poll data from");
		RegisterClassVariableComment("LBAPIConfigEntry", "enabled", "Enable / Disable an endpoint");
		RegisterClassVariableComment("LBAPIConfigEntry", "name", "Name of it for logging");
		RegisterClassVariableComment("LBAPIConfigEntry", "pollUrl", "URL to poll data from");
		RegisterClassVariableComment("LBAPIConfigEntry", "responseUrl", "URL to send back the data");
		RegisterClassVariableComment("LBAPIConfigEntry", "apiKey", "Optional API key if the endpoints requires an API key");
		RegisterClassVariableComment("LBAPIConfigEntry", "restrictedAccess", "When set to 1 some routes are going to be blocked");
		RegisterClassComment("LBAppearanceConfig", "This file contains the path to your logo, which will be displayed on some mod menus. The default path is set to `LB_Server_Logo/gui/images/logo.paa`\nA PBO with the logo inside at this location can easily be generated via [The Logo Generator](https://lbmaster.de/logo/)\nFollow the instructions there to generate the PBO");
		RegisterClassVariableComment("LBAppearanceConfig", "logoPath", "Path to the logo displayed in some mod menus. Currently only used by Advanced Groups and Virtual Garage.");
		RegisterClassComment("LBCurrencyConfig", "Shared config between all LBmaster Mods for all setting up money items");
		RegisterClassVariableComment("LBCurrencyConfig", "thousandsSeparator", "String printed every thousands value. Can be empty to only display the number. Some countryies use a dot or a comma as the seperator between each thousand value");
		RegisterClassVariableComment("LBCurrencyConfig", "currencyPrefix", "Some currencies like $ put the symbol before the number. This can be set here");
		RegisterClassVariableComment("LBCurrencyConfig", "currencySuffix", "Some currencies like € put the symbol after the number. This can be set here");
		RegisterClassVariableComment("LBCurrencyConfig", "currencyValues", "List of items, which can be used as currencies (make sure the Item has a quantity and can be stacked. Otherwise the Item will not be recognized)");
		RegisterClassVariableComment("LBCurrencyConfigEntry", "itemclassname", "The Classname (case sensitive) of the Item");
		RegisterClassVariableComment("LBCurrencyConfigEntry", "value", "Value **one** Item is worth (not a whole stack). For example there is a 1 Dollar bill and you can stack them up to 500, then the value is 1 and not 500");
		RegisterClassVariableComment("LBCurrencyConfigEntry", "depositableOnly", "Set if the currency can only be deposited, for example to deposit something like Gold Bars at the ATM, but don't give them back when withdrawing money");
		RegisterClassComment("LBIconConfig", "Icons for messages displayed at the top of the screen");
		RegisterClassVariableComment("LBIconConfig", "error", "Icon for Messages, which should not appear for the normal Player, only if something is not configured properly");
		RegisterClassVariableComment("LBIconConfig", "warning", "Icon for Messages, which warn the player about something they are doing wrong");
		RegisterClassVariableComment("LBIconConfig", "info", "Icon for Messages, which inform the player about something worked");
		RegisterClassVariableComment("LBIconConfig", "applied", "Mostly used for Server config changes being saved");
		RegisterClassVariableComment("LBIconConfig", "abort", "Icon for info messages, which are sent when something was canceled");
		RegisterClassVariableComment("LBIconConfig", "building", "Icon for info messages related to (Base)building actions");
		RegisterClassComment("LBLastPlayernames", "Save all last playernames to display them when the player is offline and makes a trap kill setup by them and for more things\nThis file should not be edited manually");
		RegisterClassVariableComment("LBLastPlayernames", "players", "This is just a list of all steamids ever joined the server");
		RegisterClassComment("LBLoggerConfig", "This file contains the setting for what messages should be logged and where they should be written to");
		RegisterClassVariableComment("LBLoggerConfig", "logLevel", "Defines how much information will be logged (0 to 7) (none to everything). (`0 = None`, `1 = Fatal`, `2 = Error`, `3 = Admin`, `4 = Info` (default), `5 = Debug`, `6 = Verbose`, `7 = Spam`)");
		RegisterClassVariableComment("LBLoggerConfig", "logFullTimestamp", "0 to only log the time of the Day in the log. 1 to log the full timestamp including the date in the log");
		RegisterClassVariableComment("LBLoggerConfig", "logFolder", "Folder where the LBmaster log will be saved to (default is the profiles folder to allow easy log rotation for the server managers)");
		RegisterClassVariableComment("LBLoggerConfig", "logToScriptlog", "Toggle if the log should be written to the scriptlog too. This does not disable the logging to the LBmaster Log");
		RegisterClassVariableComment("LBLoggerConfig", "sendCriticalErrorLogs", "When this option is enabled, specific error logs are sent to the License server for analysis. Logs are only kept for 30 Days and are only used to improve our products and make them more reliable and bugfree.");
		RegisterClassComment("LBNewModsConfig", "");
		RegisterClassVariableComment("LBNewModsConfig", "ignoredMods", "");
		RegisterClassComment("LBTooltipConfig", "");
		RegisterClassVariableComment("LBTooltipConfig", "enabled", "");
		RegisterClassComment("PlayerNameSave", "This file contains some basic info about a player");
		RegisterClassVariableComment("PlayerNameSave", "steamid", "Steamid of the Player Saved");
		RegisterClassVariableComment("PlayerNameSave", "bohemiaId", "Bohemia Guid");
		RegisterClassVariableComment("PlayerNameSave", "lastName", "Last known name, which is updated on joining");
		RegisterClassVariableComment("PlayerNameSave", "lastGroupTag", "Last known group name");
		RegisterClassVariableComment("PlayerNameSave", "groupTagVisible", "If the group tag should be shown");
		RegisterClassVariableComment("PlayerNameSave", "lastTag", "Last known Chat Tag");
		RegisterClassVariableComment("PlayerNameSave", "lastOnlineTimestamp", "Last time the player joined the server");
		RegisterClassVariableComment("PlayerNameSave", "firstJoinTimestamp", "first time the player joined the server");
		RegisterClassVariableComment("PlayerNameSave", "playSessions", "Keeps track how often a player joined the server");
		RegisterClassComment("LB_PresetLoader", "This file defines the Presets for the spawn select mod, which can be added to the layouts either via the config, or the ingame admin menu, which is recommended.\n**Important**: Make sure you don't have any circular items in the presets ! Otherwise the Server might end up in an infinite loop when trying to spawn a preset. Example `Preset_Weapons_Medium` in `Preset_Military` in `Preset_Weapons_Medium`\nI would highly recommend using the ingame Loadout editor to create your presets.\nIf you never used the Loadout Editor or setup loot presets, I would recommend watching my [Video Tutorials](https://www.youtube.com/watch?v=_Tw7BVqP_CU&list=PLbFzn5xhrn-JF19bTdmt5mjtS7iCzoy7y)");
		RegisterClassVariableComment("LB_PresetLoader", "defaultItemConfigs", "Default config used when a preset was not configured for an item. itemname is ignored. to use it, the preset must not exist and match itemname + presetName of this preset at the end. E.g. Item: \"Battery\". Presetname is \"_ATT\". Adding \"Battery_ATT\" as the item of another preset, will apply the default preset");
		RegisterClassVariableComment("LB_ItemPreset", "presetName", "Name of the preset it is referenced by. The name must be unique, otherwise the behaviour is undefined");
		RegisterClassVariableComment("LB_ItemPreset", "individualChance", "If set to 1, the chance for the preset to spawn is defined by `chance` (`0.0 = 0%`, `0.5 = 50%`, and `1.0 = 100%`). If set to 0, the chance is defined by summing up all chances of presets with also have `individualChance` set to 0 and in the same parent preset. So this basically allows to spawn one of the child presets with the option to have one spawn more often than the other. For example different scopes on weapons, where only one should be spawned, but some should be more common than others");
		RegisterClassVariableComment("LB_ItemPreset", "chance", "explained above.");
		RegisterClassVariableComment("LB_ItemPreset", "items", "List of presets, which can spawn within this preset. This will be the parent preset referenced in the `individualChance` explaination. This contains the `presetName` of either a `basePreset` or an `itemsPreset`");
		RegisterClassVariableComment("LB_ItemPreset", "minSpawnTries", "How often this preset will be spawned at least (this way you can have min and max items spawning)");
		RegisterClassVariableComment("LB_ItemPreset", "maxSpawnTries", "How often this preset will be spawned at most");
		RegisterClassVariableComment("LB_ItemPreset", "allowDuplicates", "only applies to subpresets, which have individual chance disabled. With individual chance enabled, these are not affected by this setting. Example: You spawn 2 to 3 items and have the subpresets: common, uncommon, rare, epic, legendary. When this is enabled, you can end up spawning 3x common or 2x rare and 1x common or 3x legendary, but when disabled, you can always have 1 of each maximum, because they cannot be picked again. So 1x common, 1x rare and 1x legendary, but never more than once");
		RegisterClassVariableComment("LB_ItemPreset", "itemname", "The item classname for the item, which should be spawned. E.g `TShirt_White`, `ShortJeans_Red`, `Apple`");
		RegisterClassVariableComment("LB_ItemPreset", "addToHotbar", "If the preset is spawned on a player, the item will be placed in the next free hotbar slot. So make sure the order of the presets has the items first, which should be in the first slots of the hotbar. Useful for weapons, magazines or bandages");
		RegisterClassVariableComment("LB_ItemPreset", "quantmin", "-1.0 to spawn with default quantity. Used for ammo stacks, magazines (also when attached to weapons), bottles, money etc. Otherwise it's 0.0 to 1.0 for 0% and 100% minimum quantity. So this is not an absolute number");
		RegisterClassVariableComment("LB_ItemPreset", "quantmax", "-1.0 to spawn with default quantity. Otherwise it's 0.0 to 1.0 for 0% and 100% maximum quantity. So this is not an absolute number. You can exceed this and the mod will spawn multiple stacks. you could set it to 5.0 to spawn up to 5 stacks");
		RegisterClassVariableComment("LB_ItemPreset", "spawnIn", "Very powerful option, which defines the location the item is spawned. The following options are available (make sure to use the exact lowercase name !):\n	- `any`: Tries to spawn the item in any location (attachment or cargo where attachment is tried first and then cargo) of the parent item. Useful to have items still spawning in cargo when no attachment slot is free\n	- `att`: Tries to spawn the item as attachment of the parent Item. If it failes, the item is not spawned at all\n	- `cargo`: Tries to spawn the item in the cargo space of the parent item. If it failed, the item is not spawned at all\n	- `root`: Tries to spawn the item like any, but the parent item is the root item the preset is spawned at. For example the player. This is especially useful when wanting to spawn extra mags with weapons in the player inventory. any would try to spawn the item in the cargo of the weapon and it will fail. root will spawn the item in the player inventory instead.\n	- `hands`: Tries to spawn the item in the Hands of the Player when the root entity is a player. If it's not a player or the player already has an item in hands, the item will be spawned like described in any\n	- `mag`: Tries to spawn the item in the magazine slot of the parent weapon. If the parent is not a weapon or already has a magazine, it will fail. Otherwise the weapon will be properly loaded with the mag and can be fired by the player right away. useful to be combined with the parent weapon spawning in hands and magazines in the weapon to give the player a fire ready weapon in hands. If the weapon does not have a magazine, you can also use the ammo stacks to load the internal magazine (addToHotbar and quantity does not apply to the internal magazine spawning)\n");
		RegisterClassVariableComment("LB_PresetLoader", "presetLists", "You can organize your presets into different categories. Be aware, that all presets are going to be merged into one big list internally, so no duplicate names across different categories are allowed! I highly advise to use a consistent prefix for each item and preset in each category. E.g. Category Weapons always uses WPN_ as a prefix");
		RegisterClassVariableComment("LB_Presets", "name", "Name of this category. Be aware, that in the end all categories will be merged and no duplicate names are allowed across different categories!");
		RegisterClassVariableComment("LB_Presets", "base", "List of presets, which can contain other presets or items. If you want to spawn items, you need to define a preset in the items list for each Item !");
		RegisterClassVariableComment("LB_PresetList", "presetName", "Name of the preset it is referenced by. The name must be unique, otherwise the behaviour is undefined");
		RegisterClassVariableComment("LB_PresetList", "individualChance", "If set to 1, the chance for the preset to spawn is defined by `chance` (`0.0 = 0%`, `0.5 = 50%`, and `1.0 = 100%`). If set to 0, the chance is defined by summing up all chances of presets with also have `individualChance` set to 0 and in the same parent preset. So this basically allows to spawn one of the child presets with the option to have one spawn more often than the other. For example different scopes on weapons, where only one should be spawned, but some should be more common than others");
		RegisterClassVariableComment("LB_PresetList", "chance", "explained above.");
		RegisterClassVariableComment("LB_PresetList", "items", "List of presets, which can spawn within this preset. This will be the parent preset referenced in the `individualChance` explaination. This contains the `presetName` of either a `basePreset` or an `itemsPreset`");
		RegisterClassVariableComment("LB_PresetList", "minSpawnTries", "How often this preset will be spawned at least (this way you can have min and max items spawning)");
		RegisterClassVariableComment("LB_PresetList", "maxSpawnTries", "How often this preset will be spawned at most");
		RegisterClassVariableComment("LB_PresetList", "allowDuplicates", "only applies to subpresets, which have individual chance disabled. With individual chance enabled, these are not affected by this setting. Example: You spawn 2 to 3 items and have the subpresets: common, uncommon, rare, epic, legendary. When this is enabled, you can end up spawning 3x common or 2x rare and 1x common or 3x legendary, but when disabled, you can always have 1 of each maximum, because they cannot be picked again. So 1x common, 1x rare and 1x legendary, but never more than once");
		RegisterClassVariableComment("LB_PresetList", "root", "Set this to 1 if the preset should be visible in the Spawn Select admin menu for adding it to the loadouts list of a spawn to not have too many entries in the list. Some of the Presets will be used as intermediate Presets like `Preset_Military_Clothing`, which then contains more different clothing presets to allow more precise controll over which items to spawn with their chance");
		RegisterClassVariableComment("LB_Presets", "items", "the List of items, which can have other presets as children to allow either cargo items or attachments or even weapon magazines");
		RegisterClassVariableComment("LB_ItemPreset", "itemname", "The item classname for the item, which should be spawned. E.g `TShirt_White`, `ShortJeans_Red`, `Apple`");
		RegisterClassVariableComment("LB_ItemPreset", "addToHotbar", "If the preset is spawned on a player, the item will be placed in the next free hotbar slot. So make sure the order of the presets has the items first, which should be in the first slots of the hotbar. Useful for weapons, magazines or bandages");
		RegisterClassVariableComment("LB_ItemPreset", "quantmin", "-1.0 to spawn with default quantity. Used for ammo stacks, magazines (also when attached to weapons), bottles, money etc. Otherwise it's 0.0 to 1.0 for 0% and 100% minimum quantity. So this is not an absolute number");
		RegisterClassVariableComment("LB_ItemPreset", "quantmax", "-1.0 to spawn with default quantity. Otherwise it's 0.0 to 1.0 for 0% and 100% maximum quantity. So this is not an absolute number. You can exceed this and the mod will spawn multiple stacks. you could set it to 5.0 to spawn up to 5 stacks");
		RegisterClassVariableComment("LB_ItemPreset", "spawnIn", "Very powerful option, which defines the location the item is spawned. The following options are available (make sure to use the exact lowercase name !):\n	- `any`: Tries to spawn the item in any location (attachment or cargo where attachment is tried first and then cargo) of the parent item. Useful to have items still spawning in cargo when no attachment slot is free\n	- `att`: Tries to spawn the item as attachment of the parent Item. If it failes, the item is not spawned at all\n	- `cargo`: Tries to spawn the item in the cargo space of the parent item. If it failed, the item is not spawned at all\n	- `root`: Tries to spawn the item like any, but the parent item is the root item the preset is spawned at. For example the player. This is especially useful when wanting to spawn extra mags with weapons in the player inventory. any would try to spawn the item in the cargo of the weapon and it will fail. root will spawn the item in the player inventory instead.\n	- `hands`: Tries to spawn the item in the Hands of the Player when the root entity is a player. If it's not a player or the player already has an item in hands, the item will be spawned like described in any\n	- `mag`: Tries to spawn the item in the magazine slot of the parent weapon. If the parent is not a weapon or already has a magazine, it will fail. Otherwise the weapon will be properly loaded with the mag and can be fired by the player right away. useful to be combined with the parent weapon spawning in hands and magazines in the weapon to give the player a fire ready weapon in hands. If the weapon does not have a magazine, you can also use the ammo stacks to load the internal magazine (addToHotbar and quantity does not apply to the internal magazine spawning)\n");
		RegisterClassComment("LBLasterSpawnLoadoutsConfig", "This file keeps track of the last loadout a player was spawned with when his Character spawned. It saves 1.6 seconds after the spawning to allow for mods to spawn related items on the player.\nThis is used for the Loadout Editor to preview the player when the inventory is not cleared");
		RegisterClassVariableComment("LBLasterSpawnLoadoutsConfig", "rootEntry", "Root Entry for the last Loadout (Player Object)");
		RegisterClassVariableComment("LBSpawnLoadoutEntry", "itemname", "Itemname of the entity");
		RegisterClassVariableComment("LBSpawnLoadoutEntry", "location", "Location type (Ground, Attachment, Cargo, Hands)");
		RegisterClassVariableComment("LBSpawnLoadoutEntry", "slotId", "SlotId when the item is attached");
		RegisterClassVariableComment("LBSpawnLoadoutEntry", "row", "Row Index when item is in cargo");
		RegisterClassVariableComment("LBSpawnLoadoutEntry", "column", "Column Index when item is in cargo");
		RegisterClassVariableComment("LBSpawnLoadoutEntry", "idx", "Index of the Item when item is in cargo");
		RegisterClassVariableComment("LBSpawnLoadoutEntry", "flipped", "Keep Track if the item is flipped in cargo");
		RegisterClassVariableComment("LBSpawnLoadoutEntry", "children", "All children of this Item (Attachments and Cargo items) with the same structure");
	}
}